---
name: commit-push
title: 提交代码并推送
description: 创建符合 Conventional Commits 规范的提交，并推送到远程仓库
created: 2026-01-06
updated: 2026-01-06
status: active
owner: dev-team
---

# 提交代码并推送

自动创建符合 Conventional Commits 规范的 Git 提交，并推送到远程仓库。

## Claude Code 执行指示

**重要**: 根据用户输入的语言（中文/日语/英语）进行回复。

执行此命令时，请按以下步骤操作：

### 1. 检查 Git 状态

```bash
git status
```

分析变更的文件和类型。

### 2. 分析变更类型

根据变更内容确定 commit 类型：

| 类型 | 说明 | 示例 |
|------|------|------|
| `feat` | 新功能 | 添加新功能、新组件 |
| `fix` | Bug 修复 | 修复错误、异常 |
| `docs` | 文档变更 | 更新 README、注释 |
| `refactor` | 重构 | 代码重构，不改变功能 |
| `test` | 测试相关 | 添加/修改测试 |
| `perf` | 性能优化 | 提升性能 |
| `style` | 代码风格 | 格式化、不影响代码含义 |
| `chore` | 构建/工具 | 更新依赖、配置 |

### 3. 生成 Commit 消息

**格式**: `<type>: <subject>`

**规则**:
- 使用用户输入的语言（中文/日语/英语）编写 subject
- subject 以动词开头，使用祈使句
- subject 不超过 50 字符
- subject 首字母小写（除非是专有名词）
- subject 结尾不加句号

**示例**:

```bash
# 中文示例
feat: 添加用户认证功能
fix: 修复登录超时问题
docs: 更新 API 文档

# 日语示例
feat: ユーザー認証機能を追加
fix: ログインタイムアウト問題を修正
docs: APIドキュメントを更新

# 英语示例
feat: add user authentication
fix: fix login timeout issue
docs: update API documentation
```

### 4. 添加并提交文件

```bash
# 添加所有变更
git add .

# 或添加特定文件
git add <file1> <file2>

# 创建提交（使用用户输入的语言）
git commit -m "<commit message>"
```

**可选**: 添加详细描述

```bash
git commit -m "<type>: <subject>

<详细描述>

- 详细变更点1
- 详细变更点2
- 关联 Issue: #123"
```

### 5. 推送到远程仓库

```bash
# 推送到当前分支
git push

# 或推送到指定分支
git push origin <branch-name>

# 如果是新分支，使用 -u 参数
git push -u origin <branch-name>
```

## 执行流程示例

```bash
# 1. 检查状态
git status

# 2. 查看变更内容（可选）
git diff

# 3. 添加文件
git add .

# 4. 提交（示例）
git commit -m "feat: 添加用户登录页面组件"
# 或
git commit -m "feat: ユーザーログインページコンポーネントを追加"
# 或
git commit -m "feat: add user login page component"

# 5. 推送
git push
```

## 注意事项

### ⚠️ 提交前检查

- [ ] Pre-commit hooks 已通过（如果配置）
- [ ] 代码已通过测试
- [ ] 敏感信息已排除（API密钥、密码等）
- [ ] `.env.local` 等私密文件未包含在提交中

### 📋 .gitignore 检查

确保以下文件不会被提交：

- `.env.local`
- `.env.*.local`
- `node_modules/`
- `dist/`
- `build/`
- `.DS_Store`
- `*.log`

### 🔒 敏感信息检查

```bash
# 检查是否包含敏感信息
git diff --cached | grep -i "password\|secret\|api_key\|token"
```

## 参考文档

- **Conventional Commits**: https://www.conventionalcommits.org/
- **项目开发规范**: [docs/00_policy/004_development_policy.md](../../docs/00_policy/004_development_policy.md)
- **Git 工作流**: [docs/20_guide/versioning-workflow.md](../../docs/20_guide/versioning-workflow.md)
